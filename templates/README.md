# Templates

This directory is reserved for future report templates (e.g. PDF or HTML styles).

In the current MVP, the application outputs data directly through Streamlit and offers a JSON download.
Template files may be added later to support customizable report formats.
